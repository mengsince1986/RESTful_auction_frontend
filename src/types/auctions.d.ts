type Auction = {
    auctionId: number,
    title: string,
    endDate: string,
    categoryId: number,
    reserve: number,
    sellerId: number,
    sellerFirstName: string,
    sellerLastName: string,
    numBids: number,
    highestBid: number
}